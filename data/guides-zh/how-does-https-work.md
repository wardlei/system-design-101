---
title: "HTTPS 是如何工作的？"
description: "了解 HTTPS 如何加密数据，实现互联网上的安全通信。"
image: "https://assets.bytebytego.com/diagrams/0220-how-does-https-work.png"
createdAt: "2024-03-13"
draft: false
categories:
  - security
tags:
  - "HTTPS"
  - "加密"
---

![](https://assets.bytebytego.com/diagrams/0220-how-does-https-work.png)

超文本传输安全协议（HTTPS）是超文本传输协议（HTTP）的扩展。HTTPS 使用传输层安全协议（TLS）传输加密数据。如果数据在网上被劫持，劫持者得到的只是二进制代码。

## 数据是如何加密和解密的？

第 1 步——客户端（浏览器）与服务器建立 TCP 连接。

第 2 步——客户端向服务器发送一个“client hello”。该消息包含一组必要的加密算法（密码套件）以及它所能支持的最新 TLS 版本。服务器以一个“server hello”回应，这样浏览器就能知道服务器是否支持这些算法和 TLS 版本。

随后，服务器将 SSL 证书发送给客户端。证书包含公钥、主机名、到期日期等。客户端验证该证书。

第 3 步——验证 SSL 证书后，客户端生成一个会话密钥，并用公钥对其进行加密。服务器接收加密后的会话密钥，并用私钥解密。

第 4 步——此时客户端和服务器都持有相同的会话密钥（对称加密），加密数据在一个安全的双向通道中传输。

## 为什么 HTTPS 在数据传输过程中会切换到对称加密？

主要有两个原因：

*   **安全性：** 非对称加密是单向的。这意味着如果服务器尝试把加密数据发回客户端，任何人都可以用公钥解密这些数据。

*   **服务器资源：** 非对称加密会带来相当多的数学计算开销。它不适合长时间会话中的数据传输。
